{
  "name": "SongDrive",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libasound2-dev && pnpm install --offline"
}
