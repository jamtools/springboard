import React from 'react';

/**
 * Stub FrontendRoutes component
 *
 * This is a placeholder that gets replaced by the Vite plugin's virtual module
 * at build time. For npm package builds, we provide this stub to prevent build errors.
 */
export const FrontendRoutes: React.FC = () => {
    return <div>FrontendRoutes placeholder - should be replaced by virtual module</div>;
};
