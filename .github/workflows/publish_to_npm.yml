name: Publish to NPM

on:
  push:
    tags:
      - 'v*'

permissions:
  id-token: write
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.13.2

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
            node-version: 24
            cache: 'pnpm'
            registry-url: https://registry.npmjs.org/

      - name: Install dependencies
        run: pnpm i

      - name: Build
        run: npx turbo run build --filter=springboard --filter=@springboard/vite-plugin --filter=create-springboard-app --filter=@jamtools/core

      - name: Check generated package exports
        run: npm run check:package-exports

      - name: Check types
        run: npx turbo run check-types --filter=springboard --filter=@springboard/vite-plugin --filter=@jamtools/core

      - name: Test
        run: npx turbo run test --filter=springboard --filter=@jamtools/core

      - name: Run publish script with tag
        run: ./scripts/run-all-folders.sh ${{ github.ref_name }} --mode npm --packages springboard,jamtools-core,create-springboard-app
